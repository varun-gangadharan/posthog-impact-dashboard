# GitHub personal access token (classic) with public_repo scope
# Used ONLY by server-side scripts (fetch:data). Never exposed to the browser.
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=
